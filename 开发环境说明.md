# 天工开物 Mod · 开发环境说明

> 本文档由 AI 在环境补齐阶段整理（2026-09-04），记录本项目的开发环境、已装工具、参考资料与版本约束。
> 目标：让后续每一次开发都有据可查，避免重复踩版本坑。

---

## 1. 项目定位

- **内容核心**：以宋应星《天工开物》为蓝本，把古代农业/手工业还原进 Minecraft。
- **平台**：Minecraft Java 版 **1.21.1** + **NeoForge** 加载器（1.21.1 系列最新 **21.1.249**）。
- **原则**：全部内容（程序、材质、模型、音乐）由 AI 完成，用户只提供思路。
- **用户基础**：用户无 Java 基础、无构建工具使用经验 → 每一步操作前 AI 需写明目的与操作，等用户确认。

---

## 2. 当前环境（本机）瓶颈

- **硬件**：联想 ThinkPad X240，i5-4300U @1.9GHz（Haswell，核显 HD 4400），内存极小（常驻占用 ~96%）。
- **JDK**：本机仅有 11 / 17 / **21**（默认 `java` 为 21.0.9）。**无 JDK 25**。
- **影响**：
  - 26.1.2 渲染器带不动老核显（曾崩在 `ig75icd64.dll`，`hs_err` 里有 `GL_INVALID_VALUE` + `Can't keep up!`）。
  - 因此项目**降回 1.21.1 + Java 21**：既能跑，又免去 Gradle 自动下载 JDK 25。
  - AI 常驻本身占内存较大，必要时提醒用户退出以释放内存。

---

## 3. 已安装的 AI 开发工具

### 3.1 技能（Skill）

| 技能 | 级别 | 用途 | 备注 |
|---|---|---|---|
| **minecraft-modder-neoforge** (v1.0.2, MIT) | 用户级 `~/.workbuddy/skills/` | NeoForge 代码开发指南：注册 / datagen / Mixin / 事件 / 单元测试 / 崩溃排查 | ✅ 已审计安全（纯本地、无上传/付费）。**注意：该技能默认 NeoForge 26.1.2，本项目一律钉为 1.21.1** |
| buddy-multimodal-generation（内置） | 内置 | 文生/图生 3D 模型 | 实体模型可参考，但产物非 MC Blockbench 格式 |

> **未安装**（已评估并否决）：
> - `minecraft-custom-mod-builder`：远程付费构建服务（源码上传云端、需账号+钱包+x402），且内置作弊客户端模块，不符"本地由 AI 完成"且存在代码外泄风险。
> - 官方 WorkBuddy 市场对 "pixel art / sound / blockbench" 均无结果。

### 3.2 MCP

| MCP | 状态 | 说明 |
|---|---|---|
| **Blockbench MCP** (`jasonjgardner/blockbench-mcp-plugin`, 395★) | **暂缓** | 给 Blockbench 加装 MCP 服务端，需本机装并运行 Blockbench（Electron，对 X240 偏重）。当前内容（水稻/翰墨台/残页）用 JSON 物品/方块模型即可，待加实体/复杂模型时再装。 |
| 声音生成 MCP | **不采用** | 无合适本地方案（仅有在线播放类，需外部 token）。改用本地 Python 合成 `.ogg`。 |

---

## 4. 四大内容方向的工具路线

| 方向 | 方案 | 说明 |
|---|---|---|
| **代码** | Skill：`minecraft-modder-neoforge` + NeoForge 文档 | 注册/事件/datagen 等 |
| **材质（贴图）** | `ImageGen` 文生图 → 后处理成 16×16 / 正确尺寸 PNG | 可选：`pixel-art-processing`（本地像素图后处理）+ `pixel-art-prompt-builder-skill`（提示词构建），需用时再审计安装 |
| **模型** | 物品/方块用 JSON 模型（手写或 datagen）；实体用 Blockbench（暂缓 MCP） | 1.21.1 物品模型路径：`assets/<modid>/models/item/` |
| **声音** | **本地 Python 合成**（numpy 生成波形 → pydub/ffmpeg 编码为 `.ogg`） | 完全离线；放置于 `assets/<modid>/sounds/` + `sounds.json` 定义 |

---

## 5. 关键版本约束（务必遵守）

| 项 | 值 |
|---|---|
| minecraft_version | **1.21.1** |
| neo_version | **21.1.249** |
| java.toolchain.languageVersion | **21** |
| ModDev 插件 | **2.0.146** |
| 物品模型目录 | `assets/<modid>/models/item/`（⚠️ 26.x 已改为 `assets/<modid>/items/`，不要混用） |

> ⚠️ NeoForge 官方文档（docs.neoforged.net）当前为 **26.1** 版 API。本项目是 **1.21.1（21.1.x）**，部分 API 可能有差异（如 `Item.Properties` 构造、`setId` 是否必填）。**一切以本项目实际编译/运行为准**，遇到差异以官方文档为参考、以本地编译报错为判据。

---

## 6. 常用构建/运行命令

```bash
./gradlew build        # 构建，产物在 build/libs/<mod_id>-<version>.jar
./gradlew runClient    # 启动客户端测试
./gradlew runServer    # 启动服务端（需先改 eula.txt=true、server.properties 的 online-mode=false）
./gradlew runData      # 运行数据生成（datagen）→ src/generated/resources
./gradlew test         # 单元测试
```

- `neoforge.mods.toml` 由 `gradle.properties` 注入（本项目模板在 `src/main/templates/META-INF/`）。
- 现代 NeoForge 自动生成 `pack.mcmeta`，无需手填。

---

## 7. 参考资料

- NeoForge 官方文档：https://docs.neoforged.net/ （注意版本为 26.1，API 以本项目 1.21.1 编译为准）
  - Getting Started / Mod Files / Resources / Items / Blocks
- NeoForge 迁移入门（Primers）：https://docs.neoforged.net/primer/docs/
- Porting Primers 中文版：https://gu-zt.github.io/Porting-Primers/
- Mixin 文档（Fabric 社区维护，适用所有加载器）：https://wiki.fabricmc.net/zh_cn:tutorial:mixin_introduction
- ModDevGradle：https://github.com/neoforged/ModDevGradle

---

## 8. 待办 / 下一步

- [ ] 提交当前未提交的翰墨台 GUI 改动（降级前必须，见每日记忆 2026-09-04）
- [ ] 新建 `1.21.1` 分支并在分支上改版本配置（不动 main）
- [ ] 改 `gradle.properties` + `build.gradle` 的 4 处版本，清空 `build/` 与 `run/`
- [ ] 跑一次构建 + 客户端验证降级成功
- [ ] 实体模型阶段再评估安装 Blockbench MCP
- [ ] 做材质/声音时再审计安装 pixel-art 相关技能
